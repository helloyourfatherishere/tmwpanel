<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel</title>
    <link rel="stylesheet" href="/css/order.css">
</head>
<body>
 
<div class="nav">
        <div class="heading">
            <p>Welcome To Admin Panel</p>
        </div>
        {{>n}}
    </div>
    <h1 class="heading">UNREGISTERED USER'S ORDERS</h1>
    <div class="search">
        <div class="main_form">
            <form action="/search/orderUnregister" method="GET"> 
                <input type="search" name="search" id="search"  placeholder="search orders by order id..." autocomplete="off">
                <button type="submit">Search</button>
            </form>
        </div>
    </div>
    <div class="cards_order">
    {{#each data}}
            <a href="/orderUnregiester/view/{{this._id}}" style="color: black; text-decoration: none; display: inline-block;" >
            <div class="card_order">
            <form action="/remove/unregister/{{this._id}}" method="POST">
                <button type="submit" class="fa fa-close btn_cancel"></button>
            </form>
                    <p class="p">Order Id: <span class="det">{{this._id}}</span></p>
                    <p class="p">NAME: <span class="det">{{this.name}}</span></p>
                    <p class="p">PHONE NO: <span class="det">{{this.phone}}</span></p>
                    <p class="p">EMAIL ADDRESS: <span class="det">{{this.email}}</span></p>
                    <p class="p">ADDRESS: <span class="det">{{this.address}}</p></span>    
                    <p class="p">Order Quantity: <span class="det">1</span></p>
            </div>
            </a>
    {{/each}}
    </div>
    </body>
</html>